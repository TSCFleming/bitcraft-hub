// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#nullable enable

using System;
using SpacetimeDB;
using SpacetimeDB.ClientApi;
using System.Collections.Generic;
using System.Runtime.Serialization;

namespace BitCraftGlobal.Types
{
    public sealed partial class RemoteReducers : RemoteBase
    {
        public delegate void AddOfficialTranslatorHandler(ReducerEventContext ctx, string username, string lang);
        public event AddOfficialTranslatorHandler? OnAddOfficialTranslator;

        public void AddOfficialTranslator(string username, string lang)
        {
            conn.InternalCallReducer(new Reducer.AddOfficialTranslator(username, lang), this.SetCallReducerFlags.AddOfficialTranslatorFlags);
        }

        public bool InvokeAddOfficialTranslator(ReducerEventContext ctx, Reducer.AddOfficialTranslator args)
        {
            if (OnAddOfficialTranslator == null)
            {
                if (InternalOnUnhandledReducerError != null)
                {
                    switch(ctx.Event.Status)
                    {
                        case Status.Failed(var reason): InternalOnUnhandledReducerError(ctx, new Exception(reason)); break;
                        case Status.OutOfEnergy(var _): InternalOnUnhandledReducerError(ctx, new Exception("out of energy")); break;
                    }
                }
                return false;
            }
            OnAddOfficialTranslator(
                ctx,
                args.Username,
                args.Lang
            );
            return true;
        }
    }

    public abstract partial class Reducer
    {
        [SpacetimeDB.Type]
        [DataContract]
        public sealed partial class AddOfficialTranslator : Reducer, IReducerArgs
        {
            [DataMember(Name = "username")]
            public string Username;
            [DataMember(Name = "lang")]
            public string Lang;

            public AddOfficialTranslator(
                string Username,
                string Lang
            )
            {
                this.Username = Username;
                this.Lang = Lang;
            }

            public AddOfficialTranslator()
            {
                this.Username = "";
                this.Lang = "";
            }

            string IReducerArgs.ReducerName => "add_official_translator";
        }
    }

    public sealed partial class SetReducerFlags
    {
        internal CallReducerFlags AddOfficialTranslatorFlags;
        public void AddOfficialTranslator(CallReducerFlags flags) => AddOfficialTranslatorFlags = flags;
    }
}
